<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinamobile.demo.mapper.UserInfoMapper">

    <insert id="addUserInfo" parameterType="com.chinamobile.demo.entities.UserInfo" keyProperty="id" useGeneratedKeys="true">
        insert into user_info(user_name, password, token_id)
        values(#{userName}, #{password}, #{tokenId})
    </insert>


    <select id="getUserInfo" resultType="com.chinamobile.demo.entities.UserInfo">
        select * from user_info
        <where>
            <if test= "userName != null">
                user_name=#{userName}
            </if>
            <if test= "password != null">
                and password=#{password}
            </if>
            <if test= "tokenId != null">
                and token_id=#{tokenId}
            </if>

        </where>;
    </select>


    <update id="update" parameterType="com.chinamobile.demo.entities.UserInfo">
        update user_info
        <set>
            <if test="password != null">password=#{password},</if>
            <if test="tokenId != null">token_id=#{tokenId},</if>
            <if test="expireTime != null">expire_time=#{expireTime}</if>
        </set>
        <where>
            user_name=#{userName}
            and password=#{password}
        </where>

    </update>

</mapper>


